MySQL Connector/J is declared in pom.xml and is downloaded by Maven/NetBeans.

The server and database setup require mysql-connector-j at runtime.
Clients do not connect to MySQL and do not load this driver directly.
Use NetBeans Maven project or scripts/setup-db.cmd and scripts/run-server-dashboard.cmd.
